PORT=3000

# Optional: change these before putting the site online.
ADMIN_PASSWORD=change-me
ADMIN_SECRET=replace-with-a-long-random-string

# Optional local storage location. The default is data/store.json.
STORAGE_FILE=data/store.json